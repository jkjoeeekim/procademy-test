'use client';
import React from 'react'

const ContactButton = () => {
  return (
    <div>
        <button onClick={() => console.log('clicked')}>button</button>
    </div>
  )
}

export default ContactButton
